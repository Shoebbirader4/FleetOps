Production verification on 2026-08-18: the published domain is reachable from the browser and returns the static FleetOps boot shell, but its HTML metadata still reports `meta-fleetops-release: session-transport-d2cbacf8` when opened with `?release=6e62d67b`. This indicates the public HTML/cache is older than the Fleet Manager checkpoint. Shell fetches simultaneously experienced TLS connection resets. The Fleet Manager local lifecycle passed; production rerun remains blocked by stale public HTML/edge availability and must be retried after a fresh published asset revision is confirmed.
A second browser verification after checkpoint 8ca88dd6 still returned `meta-fleetops-release: session-transport-d2cbacf8` despite the requested release query parameter. The public domain remains pinned to an older HTML revision; this is confirmed deployment/cache staleness, not a Fleet Manager workflow result.
After force-refresh checkpoint a5f172e9, the public browser response changed from `session-transport-d2cbacf8` to `fleet-manager-6e62d67b`, confirming rollout progress, but it still does not serve the newer `fleet-manager-force-8ca88dd6` marker. The edge is advancing one published revision behind the current source; production API rerun should wait until the force marker is visible.
Local rerun on 2026-08-18 using `FLEETOPS_BASE_URL=http://localhost:3000` passed every lifecycle assertion: temporary Superadmin/Auth bootstrap, organization onboarding, invitation creation/details, invited Fleet Manager redemption, vehicle/component/work-order/compliance CRUD, all role-denial checks, single-use invitation rejection, and temporary Auth/database cleanup.
After checkpoint 815bed23, a fresh public extraction returned the FleetOps shell successfully, but did not expose the release metadata; three direct HTTP/1.1 curl attempts all failed with OpenSSL SSL_ERROR_SYSCALL. The application shell is reachable through one retrieval path, while raw TLS remains unstable, so the production Fleet Manager API rerun is still not a valid pass.
Maintenance scheduling is now configured: project-level Heartbeat `fleetops-maintenance-evaluation`, task UID `QSscoBdUfDSKYjuNaT6jJa`, enabled, cron `0 */15 * * * *` UTC, POST `/api/scheduled/maintenance`. Registration is confirmed by `manus-heartbeat list`; execution history is currently empty because the job has not reached its first scheduled run, so callback execution remains pending.
After checkpoint 77fac557 and a managed-service restart on 2026-08-19, direct HTTPS retrieval still returns `fleet-manager-force-8ca88dd6` and `assets/index-CyFEnWU8.js`, while the current source marker is `fleetops-edge-sync-20260819`. Local typecheck, 62 Vitest tests, production build, and the corrected published E2E harness all pass. This confirms the remaining mismatch is public edge/cache propagation, not an application workflow failure.
